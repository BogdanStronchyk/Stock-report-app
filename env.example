# Copy this file to .env and edit values.
# Loaded automatically only if you add env_loader.py (see instructions below)

# Pipeline stage: broad | narrow | portfolio
ANALYSIS_STAGE=portfolio

# Force FMP mode (optional override): off | minimal | full
# If unset, stage mapping applies (broad->off, narrow->minimal, portfolio->full)
#FMP_MODE=minimal

# --- Yahoo / yfinance performance ---
YF_MAX_WORKERS=6
YF_HTTP_CONCURRENCY=4
YF_USE_CACHE=1
YF_CACHE_TTL_HOURS=12

# --- FMP settings ---
FMP_API_KEY=QvFGkfoZP9uFXu5fO7j3Cck1BrGZxFYY
FMP_USE_CACHE=1
FMP_CACHE_TTL_HOURS=24
FMP_MAX_REQUESTS=0
FMP_MAX_RETRIES=2
FMP_BACKOFF_BASE=1.2
FMP_DEBUG=1
FMP_USE_QUARTERLY=1
FMP_STATEMENT_LIMIT=5
FMP_USE_QUARTERLY=0

# Existing project flag
FORCE_FMP_FALLBACK=0
